before_install:
  - sdk install java 17.0.7-tem
  - sdk use java 17.0.7-tem

install:
  - ./gradlew publishToMavenLocal -x test -x :app-sample:build -x :app-sample:assemble -x :sample-utils:processor:build -x :sample-utils:processor:assemble
